# 2.2 构建基于规则的聊天机器人

在探讨了符号主义的理论挑战后，本节我们将通过一个具体的编程实践，来直观地感受基于规则的系统是如何工作的。我们将尝试复现人工智能历史上一个极具影响力的早期聊天机器人——ELIZA。

> **🎯 核心理念：Chat is All You Need**
>
> 在理解了ELIZA的设计思想后，你完全可以让AI帮你用任何语言实现它。本节的重点是**理解原理**，而非记忆代码语法。

---

## 2.2.1 ELIZA 的设计思想

ELIZA是由麻省理工学院的计算机科学家**约瑟夫·魏泽鲍姆（Joseph Weizenbaum）**于1966年发布的一个计算机程序[1]，是早期自然语言处理领域的著名尝试之一。

ELIZA并非一个单一的程序，而是一个可以执行不同"脚本"的框架。其中，最广为人知也最成功的脚本是"DOCTOR"，它模仿了一位罗杰斯学派的非指导性心理治疗师。

ELIZA的工作方式极其巧妙：它从不正面回答问题或提供信息，而是通过识别用户输入中的关键词，然后应用一套预设的转换规则，将用户的陈述转化为一个开放式的提问。

例如，当用户说"我为我的男朋友感到难过"时，ELIZA可能会识别出关键词"我为……感到难过"，并应用规则生成回应："你为什么会为你的男朋友感到难过？"

### 设计的深层含义

魏泽鲍姆的设计思想并非要创造一个真正能够"理解"人类情感的智能体，恰恰相反，他想证明的是，通过一些简单的句式转换技巧，机器可以在**完全不理解对话内容的情况下**，营造出一种"智能"和"共情"的假象。

然而，出乎他意料的是，许多与ELIZA交互过的人（包括他的秘书）都对其产生了情感上的依赖，深信它能够理解自己。这种现象后来被称为**"ELIZA效应"**。

---

## 2.2.2 模式匹配与文本替换

ELIZA的算法流程基于**模式匹配（Pattern Matching）与文本替换（Text Substitution）**，可被清晰地分解为以下四个步骤：

### 步骤1：关键词识别与排序

规则库为每个关键词（如 `mother`, `dreamed`, `depressed`）设定一个优先级。当输入包含多个关键词时，程序会选择优先级最高的关键词所对应的规则进行处理。

### 步骤2：分解规则

找到关键词后，程序使用带通配符（`*`）的分解规则来捕获句子的其余部分。

- **规则示例**：`* my *`
- **用户输入**：`"My mother is afraid of me"`
- **捕获结果**：`["", "mother is afraid of me"]`

### 步骤3：重组规则

程序从与分解规则关联的一组重组规则中，选择一条来生成回应（通常随机选择以增加多样性）。

- **规则示例**：`"Tell me more about your family."`
- **生成输出**：`"Tell me more about your family."`

### 步骤4：代词转换

在重组前，程序会进行简单的代词转换（如 `I` → `you`, `my` → `your`），以维持对话的连贯性。

### 工作流程伪代码

```typescript
function generateResponse(userInput: string): string {
  // 1. 将用户输入拆分成单词
  const words = userInput.split(' ')

  // 2. 寻找优先级最高的关键词规则
  const bestRule = findBestRule(words)
  if (!bestRule) {
    return getGenericResponse() // 例如:"Please go on."
  }

  // 3. 使用规则分解用户输入
  const decomposedParts = decompose(userInput, bestRule.pattern)
  if (!decomposedParts) {
    return getGenericResponse()
  }

  // 4. 对分解出的部分进行代词转换
  const transformedParts = transformPronouns(decomposedParts)

  // 5. 使用重组规则生成回应
  const response = reassemble(transformedParts, bestRule.responses)

  return response
}
```

通过这套机制，ELIZA成功地将复杂的自然语言理解问题，简化为了一个可操作的、基于规则的模式匹配游戏。

---

## 2.2.3 TypeScript 实现

现在，我们将上一节描述的技术原理转化为一个简单的、可运行的TypeScript程序。下面的代码实现了一个迷你版的ELIZA，它包含了一小部分规则，但足以展示其核心工作机制。

### 规则定义

```typescript
// eliza.ts - 基于规则的聊天机器人

// 定义规则类型
interface Rule {
  pattern: RegExp
  responses: string[]
}

// 定义规则库：模式(正则表达式) -> 响应模板列表
const rules: Rule[] = [
  {
    pattern: /I need (.*)/i,
    responses: [
      "Why do you need {0}?",
      "Would it really help you to get {0}?",
      "Are you sure you need {0}?"
    ]
  },
  {
    pattern: /Why don't you (.*)\?/i,
    responses: [
      "Do you really think I don't {0}?",
      "Perhaps eventually I will {0}.",
      "Do you really want me to {0}?"
    ]
  },
  {
    pattern: /Why can't I (.*)\?/i,
    responses: [
      "Do you think you should be able to {0}?",
      "If you could {0}, what would you do?",
      "I don't know -- why can't you {0}?"
    ]
  },
  {
    pattern: /I am (.*)/i,
    responses: [
      "Did you come to me because you are {0}?",
      "How long have you been {0}?",
      "How do you feel about being {0}?"
    ]
  },
  {
    pattern: /.* mother .*/i,
    responses: [
      "Tell me more about your mother.",
      "What was your relationship with your mother like?",
      "How do you feel about your mother?"
    ]
  },
  {
    pattern: /.* father .*/i,
    responses: [
      "Tell me more about your father.",
      "How did your father make you feel?",
      "What has your father taught you?"
    ]
  }
]

// 默认回复（当没有规则匹配时）
const defaultResponses = [
  "Please tell me more.",
  "Let's change focus a bit... Tell me about your family.",
  "Can you elaborate on that?",
  "Why do you say that?",
  "I see. Please continue."
]
```

### 代词转换

```typescript
// 定义代词转换规则
const pronounSwap: Record<string, string> = {
  "i": "you",
  "you": "i",
  "me": "you",
  "my": "your",
  "am": "are",
  "are": "am",
  "was": "were",
  "i'd": "you would",
  "i've": "you have",
  "i'll": "you will",
  "yours": "mine",
  "mine": "yours"
}

/**
 * 对输入短语中的代词进行第一/第二人称转换
 */
function swapPronouns(phrase: string): string {
  const words = phrase.toLowerCase().split(' ')
  const swappedWords = words.map(word => pronounSwap[word] || word)
  return swappedWords.join(' ')
}
```

### 响应生成

```typescript
/**
 * 从数组中随机选择一个元素
 */
function randomChoice<T>(array: T[]): T {
  return array[Math.floor(Math.random() * array.length)]
}

/**
 * 根据规则库生成响应
 */
function respond(userInput: string): string {
  // 遍历规则库，寻找匹配的模式
  for (const rule of rules) {
    const match = userInput.match(rule.pattern)
    if (match) {
      // 捕获匹配到的部分（如果有）
      const capturedGroup = match[1] || ''
      // 进行代词转换
      const swappedGroup = swapPronouns(capturedGroup)
      // 从模板中随机选择一个并格式化
      const template = randomChoice(rule.responses)
      const response = template.replace('{0}', swappedGroup)
      return response
    }
  }
  // 如果没有匹配任何特定规则，使用默认回复
  return randomChoice(defaultResponses)
}
```

### 主程序（Node.js环境）

```typescript
import * as readline from 'readline'

// 创建命令行交互界面
const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout
})

console.log("Therapist: Hello! How can I help you today?")
console.log("(Type 'quit' to exit)\n")

function chat() {
  rl.question("You: ", (userInput) => {
    if (userInput.toLowerCase() === 'quit') {
      console.log("Therapist: Goodbye. It was nice talking to you.")
      rl.close()
      return
    }

    const response = respond(userInput)
    console.log(`Therapist: ${response}\n`)
    chat() // 继续对话
  })
}

chat()
```

### 运行效果

```bash
Therapist: Hello! How can I help you today?
(Type 'quit' to exit)

You: I am feeling sad today.
Therapist: How long have you been feeling sad today?

You: I need some help with my project.
Therapist: Are you sure you need some help with your project?

You: My mother is not happy with my work.
Therapist: Tell me more about your mother.

You: Why can't I be more successful?
Therapist: If you could be more successful, what would you do?

You: quit
Therapist: Goodbye. It was nice talking to you.
```

---

## 2.2.4 规则系统的根本局限

通过上述的编程实践，我们可以直观地总结出规则驱动系统的根本局限性，这些局限正是对 2.1.4 节中符号主义理论挑战的直接印证：

### 缺乏语义理解

系统不理解词义。例如，面对"I am **not** happy"的输入，它仍会机械地匹配 `I am (.*)` 规则并生成语义不通的回应，因为它无法理解否定词"not"的作用。

### 无上下文记忆

系统是**无状态的（Stateless）**，每次回应仅基于当前单句输入，无法进行连贯的多轮对话。

### 规则的扩展性问题

尝试增加更多规则会导致规则库的规模爆炸式增长，规则间的冲突与优先级管理将变得极其复杂，最终导致系统难以维护。

---

## 2.2.5 ELIZA效应的启示

尽管存在这些显而易见的缺陷，ELIZA在当时却产生了著名的"**ELIZA效应**"，许多用户相信它能理解自己。这种智能的幻觉主要源于：

1. **巧妙的对话策略**：扮演被动的提问者、使用开放式模板
2. **人类的情感投射**：人们天生倾向于将智能赋予能与之交流的对象
3. **罗杰斯疗法的特点**：非指导性心理治疗本身就强调倾听和反问

ELIZA的实践清晰地揭示了符号主义方法的核心矛盾：系统看似智能的表现，完全依赖于设计者预先编码的规则。然而，面对真实世界语言的无限可能性，这种穷举式的方法注定不可扩展。**系统没有真正的理解，只是在执行符号操作，这正是其脆弱性的根源。**

---

## 💡 动手实践建议

与其手敲每一行代码，不如这样做：

**Step 1**: 把上面的结构理解清楚

**Step 2**: 用自然语言告诉 AI 你的需求：

```
我想用 TypeScript 构建一个 ELIZA 风格的聊天机器人，具备以下能力：
1. 可以识别关键词并匹配规则
2. 可以进行代词转换
3. 支持多个对话主题（工作、学习、感情）
4. 在浏览器中运行

请帮我生成完整代码。
```

**Step 3**: AI 会生成代码，你来审核和调整

**Step 4**: 遇到问题，继续问 AI

这就是 **"Chat is All You Need"** 的实践方式。

---

## 本节小结

本节我们学习了：

1. **ELIZA的设计思想**：通过模式匹配和文本替换营造智能假象
2. **核心算法**：关键词识别 → 分解 → 代词转换 → 重组
3. **TypeScript实现**：用现代语言复现经典系统
4. **根本局限**：无语义理解、无状态、难扩展
5. **ELIZA效应**：人类如何被简单的规则系统"欺骗"

这些局限性促使研究者们思考：是否存在一种更灵活、更强大的智能构建方式？下一节我们将探讨马文·明斯基的革命性思想。

---

## 参考文献

[1] WEIZENBAUM J. ELIZA: a computer program for the study of natural language communication between man and machine[J]. Communications of the ACM, 1966, 9(1): 36-45.

---

[⬅️ 上一节：符号主义时代](2.1-符号主义时代.md) | [🏠 返回目录](README.md) | [➡️ 下一节：心智社会理论](2.3-心智社会理论.md)
